import { OrkutTextPipe } from './orkut-text.pipe';

describe('OrkutTextPipe', () => {
  it('create an instance', () => {
    const pipe = new OrkutTextPipe();
    expect(pipe).toBeTruthy();
  });
});
